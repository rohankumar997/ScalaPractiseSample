
object length_of_string {

  val c = "rohan"
  def main(args: Array[String]): Unit = {
    val a = "hi my name is"
    val b : Array[String] = Array("what","are","you")
    println("length of an string: "+a.length)
    println("length of an array: "+b.size)
    printf(c)
  }
}
